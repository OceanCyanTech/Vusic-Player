<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Vusic_Player.Pages.Views.ShowModel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vusic_Player.Pages.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ani="using:CommunityToolkit.WinUI.Animations" xmlns:media="using:CommunityToolkit.WinUI.Media"
    mc:Ignorable="d">

    <Grid Margin="10, 10" ColumnSpacing="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Viewbox>
                <Grid>
                    <Border CornerRadius="100" BorderThickness="0" BorderBrush="White" Opacity="0.9 ">
                        <Border.Shadow>
                            <ThemeShadow />
                        </Border.Shadow>
                        <Border.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Edit Show Name" Name="mnftEditShowName" Click="mnftEditShowName_Click"/>
                                <MenuFlyoutItem Text="Change Show Poster" Name="mnftEditPoster" Click="mnftEditPoster_Click"  />
                            </MenuFlyout>
                        </Border.ContextFlyout>
                        <Grid>

                            <Image Source="/Assets/oceanglow.png" MaxWidth="1000" Name="imgPoster" Stretch="Uniform"></Image>
                            <Border HorizontalAlignment="Center"  x:Name="HoverOverlay" CornerRadius="20"  Margin="10, 20" VerticalAlignment="Bottom"   Padding="20"  Opacity="0.7">
                                <Border.Background>
                                    <media:BackdropBlurBrush Opacity="1" Amount="120.0" x:Name="BackdropBlur"/>
                                </Border.Background>

                                <TextBlock Foreground="White" Width="1000" Name="txtShowName" FontSize="140" TextWrapping="Wrap" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                </Grid>
            </Viewbox>


        </Grid>
        <TeachingTip Title="Rename Show" Target="{x:Bind txtShowName}" IsOpen="False" Name="ttRename">
            <StackPanel Margin="0, 20" Orientation="Vertical" Spacing="10">
                <TextBox Name="txtRenameShow" PlaceholderText="Rename show..."></TextBox>
                <Button HorizontalAlignment="Stretch" Name="btnOfficialRename" Click="btnOfficialRename_Click">
                    <Image Source="/Assets/tickicon.png" Width="20"/>
                </Button>
            </StackPanel>
        </TeachingTip>

        <ScrollViewer Grid.Column="1" Name="ShowMainPanel">
            <Grid Grid.Column="1" RowSpacing="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Name="grdGenreRelease" RowSpacing="20" Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Spacing="10" Grid.Row="0">
                        <TextBlock  Text="About" Style="{StaticResource TitleTextBlockStyle}" Grid.Row="0"></TextBlock>
                        <Button ToolTipService.ToolTip="Edit" Click="Button_Click_2"  Background="Transparent">
                            <FontIcon Glyph="d"></FontIcon>
                        </Button>
                    </StackPanel>
                    <StackPanel Name="stkReleaseDate" Orientation="Horizontal" Spacing="5" Grid.Row="1">
                        <TextBlock Text="• Release Date:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"></TextBlock>
                        <DatePicker Visibility="Collapsed" Name="dtPickerReleaseDate"></DatePicker>
                        <TextBlock Visibility="Visible" IsTextSelectionEnabled="True" Name="txtReleaseDate"></TextBlock>
                    </StackPanel>
                    <StackPanel Name="stkGenre" Orientation="Horizontal" Spacing="5" Grid.Row="2">
                        <TextBlock Text="• Genre:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"></TextBlock>
                        <TextBox AcceptsReturn="True" TextWrapping="Wrap" Name="txtGenreEdit" Visibility="Collapsed"></TextBox>
                        <TextBlock IsTextSelectionEnabled="True" Name="txtGenre"></TextBlock>
                    </StackPanel>
                </Grid>
                <MenuFlyoutSeparator Grid.Row="1"/>
                <Grid Name="grdDescription" RowSpacing="20" Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Spacing="10" Grid.Row="0">
                        <TextBlock  Text="Description" Style="{StaticResource TitleTextBlockStyle}" Grid.Row="0"></TextBlock>
                        <Button Name="btnEdit" Click="btnEdit_Click" Background="Transparent">
                            <FontIcon Glyph="d"></FontIcon>
                        </Button>
                    </StackPanel>
                    <TextBlock IsTextSelectionEnabled="True" TextWrapping="Wrap" Name="txtDescription" Grid.Row="1"></TextBlock>
                    <TextBox AcceptsReturn="True" TextWrapping="Wrap" Name="txtEditableDescription" Visibility="Collapsed" Grid.Row="1"></TextBox>
                </Grid>
                <MenuFlyoutSeparator Grid.Row="3"/>

                <Grid Name="grdCreators" RowSpacing="10" Grid.Row="6">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Spacing="10" Grid.Row="0">
                        <TextBlock Text="Creators" Style="{StaticResource TitleTextBlockStyle}" Grid.Row="0"></TextBlock>
                        <Button  Click="btnEditCreators_Click" Name="btnEditCreators" Background="Transparent">
                            <FontIcon Glyph="d"></FontIcon>
                        </Button>
                    </StackPanel>
                    <GridView  Name="grdViewCreators" Grid.Row="1" SelectionMode="None" HorizontalAlignment="Stretch">
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="Width" Value="250" />
                                <Setter Property="Margin" Value="5" />
                                <!-- Add these to fix the alignment -->
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="MinWidth" Value="0"/>
                            </Style>
                        </GridView.ItemContainerStyle>
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <Grid CornerRadius="20" HorizontalAlignment="Stretch">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                                        <ColumnDefinition Width="*"></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <PersonPicture Width="30" Grid.Column="0" Name="ppArtist" Tapped="ppArtist_Tapped"  ProfilePicture="{Binding ArtistThumbnailImage}" Margin="0, 0, 10, 0" />
                                    <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                                        <HyperlinkButton Padding="0" ToolTipService.ToolTip="{Binding ArtistName}"  Margin="0, 0, 0, 5" Foreground="{StaticResource TextFillColorSecondaryBrush}">
                                            <TextBlock Text="{Binding ArtistName}"  Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"></TextBlock>
                                        </HyperlinkButton>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>

                </Grid>
                <TeachingTip  IsOpen="False" Title="Edit Crew" Name="ttEditCrew">

                    <Grid Margin="0, 10" RowSpacing="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="100"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Grid.Row="0" Spacing="5">
                            <TextBox Name="txtAddCrewMember" PlaceholderText="Enter name..."/>
                            <Button Content="Add" Name="btnAddtoListName"  Click="btnAddToListName_Click"/>
                            <Button Content="Sort"  Click="Button_Click_5">

                            </Button>

                        </StackPanel>
                        <Button Content="Remove" Name="btnRemoveName" Click="btnRemoveName_Click" Grid.Row="1" Visibility="Collapsed"></Button>
                        <ListView CanDragItems="True" CanReorderItems="True" AllowDrop="True" SelectionMode="Multiple" SelectionChanged="lstViewNamesCrew_SelectionChanged" Grid.Row="2" Name="lstViewNamesCrew">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ContextFlyout>
                                            <MenuFlyout>

                                                <MenuFlyoutItem Text="Delete" 
                                        Icon="Delete" 
                                        Click="MenuFlyoutItem_Click_3" 
                                        Foreground="Red" />
                                            </MenuFlyout>
                                        </Grid.ContextFlyout>
                                        <TextBlock Text="{Binding Name}"/>

                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="10">
                            <Button Content="Save" Style="{StaticResource AccentButtonStyle}" Click="Button_Click_4"/>
                            <Button Content="Cancel" Click="Button_Click_3"/>
                        </StackPanel>
                    </Grid>
                </TeachingTip>
                <Grid Margin="0,  0, 15, 0" Name="grdCrew" RowSpacing="10" Grid.Row="7">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Spacing="10" Grid.Row="0">
                        <TextBlock Text="Crew" Style="{StaticResource TitleTextBlockStyle}" Grid.Row="0"></TextBlock>
                        <Button Name="btnEditCrew"  Click="btnEditCrew_Click" Background="Transparent">
                            <FontIcon Glyph="d"></FontIcon>
                        </Button>
                    </StackPanel>
                    <GridView Name="grdViewCast" Grid.Row="1" SelectionMode="None" HorizontalAlignment="Stretch">
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="Width" Value="250" />
                                <Setter Property="Margin" Value="5" />
                                <!-- Add these to fix the alignment -->
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="MinWidth" Value="0"/>
                            </Style>
                        </GridView.ItemContainerStyle>
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <Grid CornerRadius="20" HorizontalAlignment="Stretch">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                                        <ColumnDefinition Width="*"></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <PersonPicture Width="30" Grid.Column="0" Name="ppArtist" Tapped="ppArtist_Tapped"  ProfilePicture="{Binding ArtistThumbnailImage}" Margin="0, 0, 10, 0" />
                                    <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                                        <HyperlinkButton Padding="0" ToolTipService.ToolTip="{Binding ArtistName}"  Margin="0, 0, 0, 5" Foreground="{StaticResource TextFillColorSecondaryBrush}">
                                            <TextBlock Text="{Binding ArtistName}"  Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"></TextBlock>
                                        </HyperlinkButton>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>

                </Grid>
                <Grid Name="grdSeasons" RowSpacing="10" Grid.Row="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Spacing="20" Grid.Row="0">
                        <TextBlock Name="txtSeasonsHead" Text="Seasons" Style="{StaticResource TitleTextBlockStyle}" Grid.Row="0"></TextBlock>
                        <Button Margin="0, 5" VerticalAlignment="Center" CornerRadius="12"   Grid.Column="0" Name="btnAddSeason" Click="btnAddSeason_Click">
                            <StackPanel Orientation="Horizontal">
                                <Image Width="20" Source="/Assets/addicon.png"></Image>
                                <TextBlock Margin="5, 0" Text="Add Season" Style="{StaticResource BodyTextBlockStyle}"></TextBlock>
                            </StackPanel>
                        </Button>
                        <HyperlinkButton Content="View Unlinked Seasons" Name="hypViewUnlinkedSeasons" Click="hypViewUnlinkedSeasons_Click"/>
                    </StackPanel>
                    <TextBlock Name="txtSeasonCount" Text="• 0 Seasons (Loading)" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}"></TextBlock>
                    <GridView  Grid.Row="2" Visibility="Visible"  VerticalAlignment="Stretch"
 x:Name="grdViewSeasons" SelectionMode="Single" IsItemClickEnabled="True" CanDragItems="True" AllowDrop="True" CanReorderItems="True"
ItemClick="grdViewSeasons_ItemClick"  Margin="0, 20" SelectionChanged="grdViewSeasons_SelectionChanged">
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="Width" Value="140" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="MinWidth" Value="0"/>
                            </Style>
                        </GridView.ItemContainerStyle>
                        <GridView.ItemTemplate>
                            <DataTemplate >
                                <StackPanel ToolTipService.ToolTip="{Binding PlaylistName}" Orientation="Vertical" 
                        
      >
                                    <ani:Implicit.Animations>
                                        <ani:ImplicitAnimationSet>
                                            <ani:ScaleAnimation Duration="0:0:0.2" />
                                        </ani:ImplicitAnimationSet>
                                    </ani:Implicit.Animations>
                                    <StackPanel  VerticalAlignment="Stretch" Orientation="Vertical" Margin="10, 15">
                                        <StackPanel.ContextFlyout>
                                            <MenuFlyout Opened="MenuFlyout_Opened">
                                                <MenuFlyoutItem Name="mnftOpenSeason" Click="mnftOpenSeason_Click" Text="Open Season">
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="d"></FontIcon>
                                                    </MenuFlyoutItem.Icon>
                                                </MenuFlyoutItem>
                                                <MenuFlyoutItem Name="mnftUnlinkSeason" Click="mnftUnlinkSeason_Click" Text="Unlink Season">
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="d"></FontIcon>
                                                    </MenuFlyoutItem.Icon>
                                                </MenuFlyoutItem>
                                            </MenuFlyout>
                                        </StackPanel.ContextFlyout>


                                        <TextBlock Text="{Binding PlaylistName}" Style="{StaticResource SubtitleTextBlockStyle}" Visibility="Visible" TextWrapping="Wrap" />
                                        <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding PlaylistCount}" Visibility="Visible" TextWrapping="Wrap" />
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>

                </Grid>
                <MenuFlyoutSeparator Grid.Row="5"/>

            </Grid>
        </ScrollViewer>
        <ScrollViewer Name="SeasonPanel" Visibility="Collapsed" Grid.Column="1">
            <Grid RowSpacing="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>

                </Grid.RowDefinitions>
                <StackPanel Orientation="Vertical" Grid.Row="0" Spacing="20">
                    <TextBlock Name="txtSeasonHeader" Text="Season 1" Style="{StaticResource TitleLargeTextBlockStyle}" Grid.Row="0"></TextBlock>

                    <TextBlock Name="txtEpisodeCount" Text="Loading episodes..." Style="{StaticResource BodyLargeTextBlockStyle}" ></TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="1" Spacing="10">
                <Button CornerRadius="12" Style="{StaticResource AccentButtonStyle}" Grid.Row="1" Name="btnPlayAll" Click="btnPlayAll_Click">
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <FontIcon Margin="0, -4" Glyph="d"></FontIcon>
                        <TextBlock Margin="0, 3" Text="Play All"></TextBlock>
                    </StackPanel>
                    </Button>
                    <Button ToolTipService.ToolTip="File Location" Name="btnOpenFileLocation" Click="btnOpenFileLocation_Click">
                        <Image Source="/Assets/appicon.png" Width="20"/>
                    </Button>
                </StackPanel>
                <ListView Name="lstViewEpisodes" Grid.Row="2">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0, 10" ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0" RowSpacing="10">
                                    <Grid.ContextFlyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem Text="Play" Click="MenuFlyoutItem_Click_1">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="d"></FontIcon>
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                            <MenuFlyoutItem Text="File Info" Click="MenuFlyoutItem_Click">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="d"></FontIcon>
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                            <MenuFlyoutItem Text="Edit Description" Click="MenuFlyoutItem_Click">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="d"></FontIcon>
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                        </MenuFlyout>
                                    </Grid.ContextFlyout>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>

                                    </Grid.RowDefinitions>
                                    <Viewbox MaxWidth="200" HorizontalAlignment="Left">
                                        <StackPanel Orientation="Horizontal" Grid.Row="0" Spacing="20">
                                            <Button Click="Button_Click_1" ToolTipService.ToolTip="Play" CornerRadius="60" Width="40" Height="40" Style="{StaticResource AccentButtonStyle}">
                                                <FontIcon Glyph="d"></FontIcon>
                                            </Button>

                                            <TextBlock ToolTipService.ToolTip="{Binding EpisodeName}"  Style="{StaticResource TitleTextBlockStyle}" Text="{Binding EpisodeName}"></TextBlock>

                                        </StackPanel>
                                    </Viewbox>
                                    <TextBlock TextWrapping="Wrap" Text="{Binding Description}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Grid.Row="1"></TextBlock>
                                    <StackPanel Orientation="Horizontal" Spacing="10" Grid.Row="2">
                                        <Button ToolTipService.ToolTip="File Info" Background="Transparent" Grid.Row="3" Padding="0" BorderBrush="Transparent" Click="Button_Click">
                                            <Image Source="/Assets/infoicon.png" Width="20" ></Image>
                                        </Button>
                                        <TextBlock Text="{Binding Duration}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Grid.Row="2"></TextBlock>

                                    </StackPanel>

                                </Grid>
                                <Viewbox Grid.Column="1" MaxWidth="230" MaxHeight="100">
                                    <Border CornerRadius="300" BorderThickness="0" BorderBrush="White" Opacity="0.9 ">
                                        <Border.Shadow>
                                            <ThemeShadow />
                                        </Border.Shadow>
                                        <Image Source="{Binding Thumbnail}" Stretch="Uniform"></Image>
                                    </Border>
                                </Viewbox>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </ScrollViewer>
    </Grid>

</Page>
