<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Vusic_Player.Pages.Views.FavouritesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vusic_Player.Pages.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:Vusic_Player.UI.UserViews.Controls"
    mc:Ignorable="d">

    <Grid Margin="30, 30" RowSpacing="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="" Name="txtFavouritesCount"></TextBlock>
        <Button Background="Transparent" Grid.Row="1" Name="btnPlayAll" Click="btnPlayAll_Click">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <Image Width="20" Source="/Assets/play.png"/>
                <TextBlock Text="Play All"/>
            </StackPanel>
        </Button>
        <controls:ListViewMedia x:Name="lstViewFav" VisiblityofViewButton="Visible" Grid.Row="2" ItemsSource="{x:Bind FavouritesItemsCol, Mode=OneWay}"></controls:ListViewMedia>
    </Grid>
</Page>
