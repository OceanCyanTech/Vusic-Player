<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Vusic_Player.Pages.Views.FolderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vusic_Player.Pages.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:Vusic_Player.UI.UserViews.Controls"
    mc:Ignorable="d">

    <Grid RowSpacing="20" Margin="20, 20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Name="txtFolderName" MaxWidth="200" TextTrimming="CharacterEllipsis" MaxLines="2" Style="{StaticResource TitleLargeTextBlockStyle}" Grid.Row="0"></TextBlock>
            <Button Name="btnRenameFolder" ToolTipService.ToolTip="rename fol" Grid.Column="1">
                <Image Width="20" Source="/Assets/appicon.png"/>
            </Button>
            <Button Name="btnOpenFolderLoc" ToolTipService.ToolTip="Open folder locati" Grid.Column="2">
                <Image Width="20" Source="/Assets/appicon.png"/>
            </Button>
        </Grid>
        <BreadcrumbBar ItemClicked="brdcrumBarMain_ItemClicked" Name="brdcrumBarMain" Grid.Row="1" FontSize="24"></BreadcrumbBar>
        <StackPanel Orientation="Horizontal" Grid.Row="2">
            <Button Name="btnPlay" ToolTipService.ToolTip="Play Media" Visibility="Collapsed" >
                <Image Width="20" Source="/Assets/play.png"/>
            </Button>
            <Button Name="btnRenameFiles" ToolTipService.ToolTip="Rename File(s)" Visibility="Collapsed" >
                <Image Width="20" Source="/Assets/appicon.png"/>
            </Button>
          
            <Button Name="btnAddToPlaylist" ToolTipService.ToolTip="Add Selected Media to Playlist(s)" Visibility="Collapsed" >
                <Image Width="20" Source="/Assets/appicon.png"/>
            </Button>
            
            <Button Name="btnDeleteFiles" ToolTipService.ToolTip="Delete File(s)" Visibility="Collapsed" >
                <Image Width="20" Source="/Assets/appicon.png"/>
            </Button>
            <Button Name="btnCreateShow" ToolTipService.ToolTip="Create a Show from this folder" Visibility="Visible" >
                <Image Width="20" Source="/Assets/appicon.png"/>
            </Button>
        </StackPanel>
        <controls:ListViewMedia MediaSource="ArtistNoRearrange" Grid.Row="3" x:Name="lstViewMain" FolderFileProperties="Visible" IsFolderItems="True"/>
        <TextBlock Grid.Row="4" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Name="txtFolderInfo"/>
    </Grid>
</Page>
