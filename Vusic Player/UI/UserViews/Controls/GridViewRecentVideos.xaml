<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Vusic_Player.UI.UserViews.Controls.GridViewRecentVideos"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vusic_Player.UI.UserViews.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <GridView ItemClick="grdViewAllRecentVideo_ItemClick" SelectionMode="{x:Bind GridSelectionMode, Mode=OneWay}" x:Name="grdViewAllRecentVideo"  Visibility="Visible" ItemsSource="{x:Bind ItemsSource, Mode=OneWay}" IsItemClickEnabled="True" Grid.Row="2">
        
            <GridView.ItemTemplate>
                <DataTemplate >
                    <Grid>
                    <StackPanel ToolTipService.ToolTip="{Binding HoverText}" Width="200" Height="200" Spacing="10" Orientation="Vertical" Margin="10, 10">
                        <StackPanel.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Play" Click="mnftPlayRecents_Click" Name="mnftPlayRecents">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE8DA;"></FontIcon>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="Remove from Recents" Click="mnftRemoveFromRecents_Click" Name="mnftRemoveFromRecents"  Icon="Remove"></MenuFlyoutItem>
                                <MenuFlyoutItem Text="Go to File Location" Click="mnftGoToFileLocation_Click" Name="mnftGoToFileLocation" >
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE8DA;"></FontIcon>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="Add to Queue" Name="mnftAddToQueue" Click="mnftAddToQueue_Click" >
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE8DA;"></FontIcon>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="View File Info" Name="mnftViewFileInfo" Click="mnftViewFileInfo_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE946;"></FontIcon>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </StackPanel.ContextFlyout>
                     
                        <Image Source="{Binding Thumbnail}" Width="200" Height="100" Stretch="UniformToFill"/>

                        <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Grid.Row="1" Text="{Binding FileName}"   TextWrapping="Wrap" />
                        <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding FolderName}" Visibility="Visible" TextWrapping="Wrap" />
                    </StackPanel>
                        <ProgressRing Visibility="{Binding LoadingVisibility}" IsIndeterminate="True"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>

        </GridView>

    </Grid>
</UserControl>
