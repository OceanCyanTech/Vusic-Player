<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Vusic_Player.UI.UserViews.Controls.OceanDialogControls.OnlineLyrics"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vusic_Player.UI.UserViews.Controls.OceanDialogControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowSpacing="10" Margin="0, 30" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel HorizontalAlignment="Stretch" Orientation="Horizontal" Grid.Row="0" Spacing="10">
            <TextBox HorizontalAlignment="Stretch" Text="{x:Bind mediacontroller.MediaDisplayName, Mode=OneWay}" PlaceholderText="Enter song name to find lyrics..." Name="txtQuery"></TextBox>
            <Button Style="{StaticResource AccentButtonStyle}" Name="btnSearch"  Click="btnSearch_Click">
                <TextBlock Name="txtBtnFind" Text="Find"/>
            </Button>
            <Button Visibility="Collapsed" Content="Save Selected Lyrics to File" Click="btnSaveSelectedLyricsToFile_Click" Name="btnSaveSelectedLyricsToFile" >
            </Button>
            <Button Visibility="Collapsed" Name="btnCopyLyrics" Click="btnCopyLyrics_Click" Content="Copy Selected Lyrics"/>
        </StackPanel>
        <InfoBar Name="ifbError" Grid.Row="1" IsOpen="False"/>
        <TextBlock Name="txtLoading" Grid.Row="2"/>
        <TextBlock Name="txtSearchResHeader" Visibility="Collapsed" Style="{StaticResource BodyLargeStrongTextBlockStyle}" Text="Search Results:" Grid.Row="3"/>
        <ListView SelectionMode="Single" IsItemClickEnabled="True"  ItemClick="lstViewQueryResults_ItemClick" Grid.Row="4 " Name="lstViewQueryResults">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnSpacing="30">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding TrackName}"/>
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Grid.Column="1" Text="{Binding ArtistName}"/>
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}"  Grid.Column="2" Text="{Binding AlbumName}"/>
                        <TextBlock ToolTipService.ToolTip="{Binding StringDuration}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"  Grid.Column="3" Text="{Binding StringDuration}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <MenuFlyoutSeparator Grid.Row="5"/>
        <TextBlock Visibility="Collapsed" HorizontalAlignment="Center" Text="Lyrics" Name="txtLyricHeader" Style="{StaticResource SubheaderTextBlockStyle}" Grid.Row="6"></TextBlock>
        <ScrollViewer Grid.Row="7">
            <RichTextBlock Name="txtLyricsFull" 
      FontSize="24"
       HorizontalAlignment="Center" 
       TextAlignment="Center"
       TextWrapping="Wrap"/>
        </ScrollViewer>
        <TeachingTip Name="ttJustSaved">
            <HyperlinkButton Name="hypPathSaved" Click="hypPathSaved_Click"/>
        </TeachingTip>
    </Grid>
</UserControl>
