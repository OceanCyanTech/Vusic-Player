<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Vusic_Player.UI.UserViews.Controls.OceanDialogControls.OnlineArtistPicture"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vusic_Player.UI.UserViews.Controls.OceanDialogControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Name="grdFindOnline" Margin="0, 10">
        <ScrollViewer>
            <StackPanel  Orientation="Vertical" Spacing="15">
                <InfoBar Name="ifbNoInternet" IsOpen="False">
                    <StackPanel Orientation="Vertical" Spacing="5">
                        <TextBlock Name="ifbMessage"></TextBlock>
                        <Button Margin="0, 7"  Name="ifbActionButton"></Button>
                    </StackPanel>
                </InfoBar>
                <StackPanel Orientation="Horizontal" Spacing="5" HorizontalAlignment="Center">
                    <AutoSuggestBox CornerRadius="12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" Background="Transparent" QueryIcon="Find" Width="250"  Name="txtArtistToFind" PlaceholderText="Artist name...."></AutoSuggestBox>
                    <Button CornerRadius="12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" Background="Transparent"  Name="btnFindOnline" Click="btnFindOnline_Click">
                        <StackPanel Orientation="Horizontal">
                            <Image Width="20" Source="/Assets/searchicon.png" />
                            <TextBlock Name="txtFindOnlineButton" Margin="5,2" Text="Find" />
                        </StackPanel>
                    </Button>
                </StackPanel>
                <Button CornerRadius="12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" Background="Transparent"  IsEnabled="False" Name="btnShowMoreResults" Visibility="Visible" Content="Show more results" Click="btnShowMoreResults_Click"></Button>
                <ProgressBar Name="prgWaitOnline" IsIndeterminate="True" Visibility="Collapsed" Width="100"></ProgressBar>
                <TextBlock Name="txtWaitOnline" Text="Finding online..." Visibility="Collapsed"></TextBlock>

                <GridView x:Name="ImageGrid"
                  IsItemClickEnabled="True" Width="400"
                  SelectionMode="Single" >

                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="4" />
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>

                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="10"
                            Margin="5"
                            Background="Gray">

                                <Image Width="150"
                               Height="150"
                               Stretch="Uniform"
                               Source="{Binding}" ImageFailed="Image_ImageFailed">
                                    <Image.ContextFlyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem Text="Set Image" Click="MenuFlyoutItem_Click">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="&#xE8DA;"></FontIcon>
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                            <MenuFlyoutItem Text="Download Image" Visibility="Collapsed" Click="MenuFlyoutItem_Click_1">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="&#xE8DA;"></FontIcon>
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                        </MenuFlyout>
                                    </Image.ContextFlyout>
                                </Image>
                            </Border>
                        </DataTemplate>
                    </GridView.ItemTemplate>

                </GridView>
                <Button CornerRadius="12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" Background="Transparent"   Click="Button_Click">
                    <StackPanel Orientation="Horizontal">
                        <Image Width="25" Source="/Assets/addimgicon.png" />
                        <TextBlock Margin="5,2" Text="Find on Disk instead" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </ScrollViewer>
    </Grid>

</UserControl>
