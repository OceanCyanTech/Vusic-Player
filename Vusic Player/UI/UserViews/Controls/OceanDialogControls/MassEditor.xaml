<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Vusic_Player.UI.UserViews.Controls.OceanDialogControls.MassEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vusic_Player.UI.UserViews.Controls.OceanDialogControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local1="using:Vusic_Player.UI.UserViews.Controls"
    mc:Ignorable="d">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <local1:ListViewMedia ExtraOptions="Collapsed" Grid.Column="0" x:Name="lstViewDisplay"></local1:ListViewMedia>
        <ScrollViewer Grid.Column="1">
            <Grid Grid.Column="1" Margin="10, 0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <TextBlock  Name="txtTitle" Style="{StaticResource SubtitleTextBlockStyle}" Grid.Row="0"></TextBlock>
                <Grid Grid.Row="1" RowSpacing="10">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition  Height="Auto"></RowDefinition>
                        <RowDefinition  Height="Auto"></RowDefinition>
                        <RowDefinition Height="170"></RowDefinition>
                    </Grid.RowDefinitions>
                    <InfoBar IsOpen="False" Name="ifbInformation" Severity="Success" Grid.Row="0"></InfoBar>
                    <TextBlock Text="0 items selected" Name="txtItemCount" Grid.Row="1" Style="{StaticResource SubtitleTextBlockStyle}"></TextBlock>
                    <TextBox PlaceholderText="Type an album name..." CornerRadius="12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" Header="Album Name" Name="txtAlbum" Grid.Row="2"></TextBox>
                    <Button CornerRadius="12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" Grid.Row="3" Content="Set" Name="btnSetAlbum" Click="btnSetAlbum_Click" HorizontalAlignment="Stretch"></Button>
                    <TextBox CornerRadius="12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" PlaceholderText="Type artist names..." Header="Artist Name" Name="txtArtist" Grid.Row="4"></TextBox>
                    <Button Name="btnSetArtist" Click="btnSetArtist_Click" CornerRadius="12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" Grid.Row="5" Content="Set" HorizontalAlignment="Stretch"></Button>

                    <ListView SelectionMode="Multiple" Grid.Row="8" Name="lstViewPlaylists" Header="Add to Selected Playlists"></ListView>
                    <Rectangle  Height="1" Fill="{ThemeResource SurfaceStrokeColorDefaultBrush}" Grid.Row="6"></Rectangle>
                    <Button Name="btnAddtoPlaylist" Click="btnAddtoPlaylist_Click" CornerRadius="12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" Grid.Row="7" Content="Add to Playlist" HorizontalAlignment="Stretch"></Button>

                </Grid>

            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
