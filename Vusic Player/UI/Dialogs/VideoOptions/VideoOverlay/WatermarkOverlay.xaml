<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Vusic_Player.UI.Dialogs.VideoOptions.VideoOverlay.WatermarkOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vusic_Player.UI.Dialogs.VideoOptions.VideoOverlay"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Watermark Overlay" Style="{StaticResource BodyLargeStrongTextBlockStyle}"/>
        <TextBlock Text="Display a watermark overlay with custom text or a running timecode on the video." Grid.Row="1"/>
        <TextBlock Text="Timecode" Grid.Row="2" Style="{StaticResource BodyStrongTextBlockStyle}"/>

        <CheckBox Grid.Row="3" VerticalAlignment="Center" Content="Display Running Timecodes" Name="chkDisplayRunningTimeCode"/>
        <Grid Grid.Row="4" ColumnSpacing="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid RowSpacing="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button Content="Add new Timecoded Label to Display" Name="btnNewTimeCodedLabel" Visibility="Visible" Click="btnNewTimeCodedLabel_Click"/>
                <ListView SelectionChanged="lstViewAddedTimeCodeLabels_SelectionChanged" ItemsSource="{x:Bind timestamplabels, Mode=OneWay}" Grid.Row="1" Name="lstViewAddedTimeCodeLabels">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnSpacing="50">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock VerticalAlignment="Center" Text="{Binding Label}"/>
                                <TextBlock VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding Position}" Grid.Column="1"/>
                                <CheckBox Content="Show" Grid.Column="2" IsChecked="{Binding IsShown}"/>
                            </Grid>

                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </Grid>
            <StackPanel VerticalAlignment="Center" Visibility="Visible" Grid.Column="1" Orientation="Vertical" Spacing="10" Grid.Row="4">
                <ComboBox Grid.Row="4" SelectedIndex="0" VerticalAlignment="Center" Header="Position">
                    <ComboBox.Items>
                        <ComboBoxItem Content="Top Left"/>
                        <ComboBoxItem Content="Top Center"/>
                        <ComboBoxItem Content="Top Right"/>
                        <ComboBoxItem Content="Left"/>
                        <ComboBoxItem Content="Centre"/>
                        <ComboBoxItem Content="Right"/>
                        <ComboBoxItem Content="Bottom Left"/>
                        <ComboBoxItem Content="Bottom Center"/>
                        <ComboBoxItem Content="Bottom Right"/>
                    </ComboBox.Items>
                </ComboBox>
                <ComboBox Grid.Row="5" SelectedIndex="0" VerticalAlignment="Center" Header="Mode">
                    <ComboBox.Items>
                        <ComboBoxItem Content="Elapsed Time of Video"/>
                        <ComboBoxItem Content="Remaining Time of Video"/>
                        <ComboBoxItem Content="Remaining Time of Video"/>
                        <ComboBoxItem Content="Current Time vs Total Duration"/>
                        <ComboBoxItem Content="Current System Time"/>
                        <ComboBoxItem Content="Custom Initial Offset"/>
                        <ComboBoxItem Content="Running Frames (Frame Rate to be Selected)"/>
                        <ComboBoxItem Content="Musical Timecode (Bars and Beats)"/>
                    </ComboBox.Items>
                </ComboBox>
                <ComboBox Name="cmbFormat" SelectedIndex="0"  Visibility="Visible" Grid.Row="5" VerticalAlignment="Center" Header="Format">
                    <ComboBox.Items>
                        <ComboBoxItem Content="HH:MM:SS.FF"/>
                        <ComboBoxItem Content="HH:MM:SS:FF"/>

                        <ComboBoxItem Content="Drop Frame (HH:MM:SS;FF)"/>
                        <ComboBoxItem Content="Current Time vs Total Duration"/>
                        <ComboBoxItem Content="Current System Time"/>
                        <ComboBoxItem Content="Custom Initial Offset"/>
                    </ComboBox.Items>
                </ComboBox>

                <NumberBox Header="Offset"/>
                <Button Name="btnFontSettings" Content="Font Settings">
                    <Button.Flyout>
                        <Flyout>
                            <StackPanel Orientation="Vertical">
                                
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                </Button>
            </StackPanel>

        </Grid>
        
    </Grid>
</UserControl>
