<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Vusic_Player.UI.Dialogs.VideoOptions.Audio.AudioGeneral.Pitch"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vusic_Player.UI.Dialogs.VideoOptions.Audio.AudioGeneral"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:oceancyan="using:Vusic_Player.UI.UserViews.Controls"
    mc:Ignorable="d">

    <StackPanel Spacing="8" Name="stkPitch" Grid.Row="6">
        <Grid>
            <TextBlock Text="Audio Pitch" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
            <TextBlock Text="{Binding ElementName=PitchSlider, Path=Value}" 
                                       HorizontalAlignment="Right" Foreground="{ThemeResource SystemAccentColor}"/>
        </Grid>
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <oceancyan:OceanSlider  IsTooltipEnabled="False" ValueChanged="PitchSlider_ValueChanged" x:Name="PitchSlider" Minimum="0.1" Margin="5, 0" Maximum="8.0" Value="{x:Bind mediacontroller.PitchValue, Mode=TwoWay}"/>
            <Button Grid.Column="1" Content="Reset" Name="btnPitchReset" Click="btnPitchReset_Click"/>
        </Grid>
    </StackPanel>
</UserControl>
